// Generated by `polycast capture --from raycast-snippets`.
// The source export stays outside Git. Regenerate this file instead of editing it.

import { defineCommand } from "../../src/define.ts";

export default defineCommand({
  "id": "raycast-snippet-repo-hygiene-for-smart-models-04369ad34f",
  "title": "Repo Hygiene for Smart Models",
  "description": "Static text captured from a Raycast snippet export.",
  "modality": "none",
  "body": {
    "lang": "node",
    "source": "process.stdout.write(\"Goal: Orchestrate end-to-end repository hygiene, PR merge-readiness triage, subagent review remediation, and safe branch/worktree lifecycle cleanup.\\nSuccess means:\\n- Open PRs are evaluated for CI status, approval state, and base-branch conflicts\\n- Actionable review feedback and failing tests are delegated to isolated subagents and verified green\\n- Ready PRs land in topological dependency order with downstream rebases\\n- Ghost worktrees and stale remote-tracking refs are pruned\\n- Merged local branches are cleanly deleted (`-d`), and stale unmerged branches (>90d) are tagged (`archive/*`) or deleted\\n- Protected branches (`main`, `master`, `develop`, `staging`) and active branches remain untouched\\n- Hard deletions (`-D`), unmerged branch drops, and disputed PR merges pause for explicit user confirmation\\nStop when: Approved PRs are landed, delegated fixes pass verification, orphaned/merged refs are cleaned up, and a concise repository status report is returned.\\nWorkflow:\\n1. Audit & Baseline: Run `git fetch --all --prune` and `git worktree prune`. Inspect dirty working trees, active worktrees, and branch commit recency.\\n2. Triage PRs & Build Merge DAG: Evaluate open PRs across approvals, CI checks, and mergeability. Order ready PRs into a dependency DAG (base/independent PRs first, dependent PRs rebased).\\n3. Dispatch Subagent Fixes: Route actionable review comments, failing tests, or merge conflicts to isolated subagents with target file scopes and verification criteria. Await green CI before promoting to the merge queue.\\n4. Plan & Gate: Present a structured action matrix categorizing refs (Merged, Stale >90d, Active, Protected) alongside the proposed PR landing sequence. Request explicit confirmation before running hard deletes (`-D`) or archiving unmerged branches.\\n5. Execute Landing & Cleanup:\\n   - Squash/merge approved PRs in dependency sequence, rebasing dependent branches as each PR lands.\\n   - Mass delete merged local branches: `git branch --merged | grep -vE '^\\\\*|main|master|develop|staging' | xargs -n 1 git branch -d`.\\n   - Apply archive tags (`archive/<branch-name>`) and delete approved stale refs.\\n   - Clean up local branches whose remotes are marked `[gone]`.\\n6. Handoff: Return a structured summary detailing landed PRs, subagent fixes, archived/deleted branches, pruned worktrees, and final repository status.\");"
  },
  "targets": [
    "raycast-snippet"
  ],
  "x": {
    "raycast": {
      "snippet": {
        "text": "Goal: Orchestrate end-to-end repository hygiene, PR merge-readiness triage, subagent review remediation, and safe branch/worktree lifecycle cleanup.\nSuccess means:\n- Open PRs are evaluated for CI status, approval state, and base-branch conflicts\n- Actionable review feedback and failing tests are delegated to isolated subagents and verified green\n- Ready PRs land in topological dependency order with downstream rebases\n- Ghost worktrees and stale remote-tracking refs are pruned\n- Merged local branches are cleanly deleted (`-d`), and stale unmerged branches (>90d) are tagged (`archive/*`) or deleted\n- Protected branches (`main`, `master`, `develop`, `staging`) and active branches remain untouched\n- Hard deletions (`-D`), unmerged branch drops, and disputed PR merges pause for explicit user confirmation\nStop when: Approved PRs are landed, delegated fixes pass verification, orphaned/merged refs are cleaned up, and a concise repository status report is returned.\nWorkflow:\n1. Audit & Baseline: Run `git fetch --all --prune` and `git worktree prune`. Inspect dirty working trees, active worktrees, and branch commit recency.\n2. Triage PRs & Build Merge DAG: Evaluate open PRs across approvals, CI checks, and mergeability. Order ready PRs into a dependency DAG (base/independent PRs first, dependent PRs rebased).\n3. Dispatch Subagent Fixes: Route actionable review comments, failing tests, or merge conflicts to isolated subagents with target file scopes and verification criteria. Await green CI before promoting to the merge queue.\n4. Plan & Gate: Present a structured action matrix categorizing refs (Merged, Stale >90d, Active, Protected) alongside the proposed PR landing sequence. Request explicit confirmation before running hard deletes (`-D`) or archiving unmerged branches.\n5. Execute Landing & Cleanup:\n   - Squash/merge approved PRs in dependency sequence, rebasing dependent branches as each PR lands.\n   - Mass delete merged local branches: `git branch --merged | grep -vE '^\\*|main|master|develop|staging' | xargs -n 1 git branch -d`.\n   - Apply archive tags (`archive/<branch-name>`) and delete approved stale refs.\n   - Clean up local branches whose remotes are marked `[gone]`.\n6. Handoff: Return a structured summary detailing landed PRs, subagent fixes, archived/deleted branches, pruned worktrees, and final repository status."
      }
    }
  },
  "author": "polycast capture"
});
