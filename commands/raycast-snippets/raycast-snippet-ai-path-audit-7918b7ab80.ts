// Generated by `polycast capture --from raycast-snippets`.
// The source export stays outside Git. Regenerate this file instead of editing it.

import { defineCommand } from "../../src/define.ts";

export default defineCommand({
  "id": "raycast-snippet-ai-path-audit-7918b7ab80",
  "title": "AI: PATH Audit",
  "description": "Static text captured from a Raycast snippet export.",
  "modality": "none",
  "body": {
    "lang": "node",
    "source": "process.stdout.write(\"  Audit my interactive zsh PATH end-to-end on macOS (Apple Silicon, Homebrew at /opt/homebrew).\\n  Goal: trace every PATH mutation from shell start to prompt-ready, find\\n  inefficiencies/misconfigurations, and recommend changes for faster resolution, cleaner\\n  startup, and easier long-term management.\\n  Known context (verify, don't assume):\\n  - Main config: ~/.zshrc (topic loader from ~/.dotfiles)\\n  - Early PATH files: ~/.dotfiles/*/path.zsh (homebrew, development, python, yarn, bin)\\n  - Canonical reorder block in ~/.zshrc (~\\\"Canonical interactive PATH order\\\") using typeset -U\\n  path + mise shims first\\n  - Additional inline PATH exports later in ~/.zshrc (kaku, ~/.agents/tools, superset,\\n  tether-ntfy, etc.)\\n  - Tool managers: mise, bun, pnpm, cargo, possible Volta/NVM remnants\\n  Workflow:\\n  1. Research / trace (read-only first)\\n     - Start a fresh interactive zsh and capture:\\n       - `print -lr -- path` (final order with indices)\\n       - `typeset -p path` / duplicate check\\n       - `time zsh -lic 'true'` (baseline startup)\\n     - Build a ordered timeline of PATH mutations:\\n       inherited env → ~/.zprofile (if any) → path.zsh files (load order) → rest of ~/.zshrc →\\n  deferred hooks (precmd) if any PATH changes\\n     - For each entry, record: source file:line, mechanism (path+=, export PATH=,\\n  path_prepend), exists on disk?, shadowed duplicate tool?\\n  2. Analysis — classify findings by severity:\\n     - CRITICAL: broken/missing dirs, wrong binary winning (e.g. brew node over mise/bun),\\n  security-sensitive paths\\n     - HIGH: duplicate managers (Volta+NVM+mise+bun all on PATH), dead entries, PATH rebuilt\\n  multiple times\\n     - MEDIUM: late inline exports bypassing canonical reorder, inconsistent prepend vs append,\\n   management sprawl\\n     - LOW: cosmetic ordering, harmless duplicates after typeset -U\\n  3. Performance / efficiency checks\\n     - Count PATH length and unique entries\\n     - Identify entries that force slow filesystem lookups (non-existent dirs, network mounts)\\n     - Spot tools resolved only after many hops (`type -a node`, `type -a python`, `type -a\\n  wt`, `type -a git`)\\n     - Note startup cost contributors (mise fork, repeated hash -r, redundant PATH rebuilds)\\n  4. Management recommendations\\n     - Propose single ownership model (mise-first for runtime tools; Homebrew for system libs)\\n     - Move stray ~/.zshrc PATH lines into appropriate ~/.dotfiles/*/path.zsh\\n     - Keep one canonical reorder section; document precedence rules in comments\\n     - Suggest deduped helper (path_prepend) vs raw `export PATH=...`\\n  Deliverables:\\n  A) PATH trace timeline (file:line → effect)\\n  B) Final PATH table: index | entry | exists? | first resolver for key tools\\n  C) Ranked issue list with severity + fix suggestion\\n  D) Minimal patch plan (smallest safe diff) — DO NOT apply until I approve\\n  E) Optional: before/after startup timing if changes are made\\n  Acceptance criteria:\\n  - Every PATH entry traced to a source or marked \\\"inherited\\\"\\n  - At least 3 concrete findings with evidence (command output)\\n  - Fix plan respects existing policy: mise shims > bun > homebrew for Node/tooling\\n  - No unrelated zsh changes (aliases, completions, secrets)\\n  Do NOT:\\n  - Change git/worktrunk/shell integration unless PATH-related\\n  - Remove tools without confirming which resolver should win\\n  - Apply edits without showing diff first\\n  - Optimize unrelated shell startup (prompt, secrets, hooks) unless they mutate PATH\\n  Verification (required before claiming done):\\n  - Fresh `zsh -lic 'print -lr -- path'` matches expected order\\n  - `command -v` for node, python, bun, wt, codex, gh shows intended binaries\\n  - No new dead PATH entries introduced\");"
  },
  "targets": [
    "raycast-snippet"
  ],
  "x": {
    "raycast": {
      "snippet": {
        "text": "  Audit my interactive zsh PATH end-to-end on macOS (Apple Silicon, Homebrew at /opt/homebrew).\n  Goal: trace every PATH mutation from shell start to prompt-ready, find\n  inefficiencies/misconfigurations, and recommend changes for faster resolution, cleaner\n  startup, and easier long-term management.\n  Known context (verify, don't assume):\n  - Main config: ~/.zshrc (topic loader from ~/.dotfiles)\n  - Early PATH files: ~/.dotfiles/*/path.zsh (homebrew, development, python, yarn, bin)\n  - Canonical reorder block in ~/.zshrc (~\"Canonical interactive PATH order\") using typeset -U\n  path + mise shims first\n  - Additional inline PATH exports later in ~/.zshrc (kaku, ~/.agents/tools, superset,\n  tether-ntfy, etc.)\n  - Tool managers: mise, bun, pnpm, cargo, possible Volta/NVM remnants\n  Workflow:\n  1. Research / trace (read-only first)\n     - Start a fresh interactive zsh and capture:\n       - `print -lr -- path` (final order with indices)\n       - `typeset -p path` / duplicate check\n       - `time zsh -lic 'true'` (baseline startup)\n     - Build a ordered timeline of PATH mutations:\n       inherited env → ~/.zprofile (if any) → path.zsh files (load order) → rest of ~/.zshrc →\n  deferred hooks (precmd) if any PATH changes\n     - For each entry, record: source file:line, mechanism (path+=, export PATH=,\n  path_prepend), exists on disk?, shadowed duplicate tool?\n  2. Analysis — classify findings by severity:\n     - CRITICAL: broken/missing dirs, wrong binary winning (e.g. brew node over mise/bun),\n  security-sensitive paths\n     - HIGH: duplicate managers (Volta+NVM+mise+bun all on PATH), dead entries, PATH rebuilt\n  multiple times\n     - MEDIUM: late inline exports bypassing canonical reorder, inconsistent prepend vs append,\n   management sprawl\n     - LOW: cosmetic ordering, harmless duplicates after typeset -U\n  3. Performance / efficiency checks\n     - Count PATH length and unique entries\n     - Identify entries that force slow filesystem lookups (non-existent dirs, network mounts)\n     - Spot tools resolved only after many hops (`type -a node`, `type -a python`, `type -a\n  wt`, `type -a git`)\n     - Note startup cost contributors (mise fork, repeated hash -r, redundant PATH rebuilds)\n  4. Management recommendations\n     - Propose single ownership model (mise-first for runtime tools; Homebrew for system libs)\n     - Move stray ~/.zshrc PATH lines into appropriate ~/.dotfiles/*/path.zsh\n     - Keep one canonical reorder section; document precedence rules in comments\n     - Suggest deduped helper (path_prepend) vs raw `export PATH=...`\n  Deliverables:\n  A) PATH trace timeline (file:line → effect)\n  B) Final PATH table: index | entry | exists? | first resolver for key tools\n  C) Ranked issue list with severity + fix suggestion\n  D) Minimal patch plan (smallest safe diff) — DO NOT apply until I approve\n  E) Optional: before/after startup timing if changes are made\n  Acceptance criteria:\n  - Every PATH entry traced to a source or marked \"inherited\"\n  - At least 3 concrete findings with evidence (command output)\n  - Fix plan respects existing policy: mise shims > bun > homebrew for Node/tooling\n  - No unrelated zsh changes (aliases, completions, secrets)\n  Do NOT:\n  - Change git/worktrunk/shell integration unless PATH-related\n  - Remove tools without confirming which resolver should win\n  - Apply edits without showing diff first\n  - Optimize unrelated shell startup (prompt, secrets, hooks) unless they mutate PATH\n  Verification (required before claiming done):\n  - Fresh `zsh -lic 'print -lr -- path'` matches expected order\n  - `command -v` for node, python, bun, wt, codex, gh shows intended binaries\n  - No new dead PATH entries introduced"
      }
    }
  },
  "author": "polycast capture"
});
