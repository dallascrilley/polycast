// Generated by `polycast capture --from raycast-snippets`.
// The source export stays outside Git. Regenerate this file instead of editing it.

import { defineCommand } from "../../src/define.ts";

export default defineCommand({
  "id": "raycast-snippet-cleanup-back-to-main-orca-abff4d0a14",
  "title": "Cleanup back to main orca",
  "description": "Static text captured from a Raycast snippet export.",
  "modality": "none",
  "body": {
    "lang": "node",
    "source": "process.stdout.write(\"I'm done with this branch. Close it out safely, integrate it, and move me onto `main`.\\nDefault: land the plane first. Always finish integration before local cleanup. Do not delete branches or remove worktrees while work is still uncommitted, unmerged, or unpushed unless I explicitly say otherwise.\\n## Orca CLI requirement\\nIf this task is being performed through the Orca CLI:\\n* Use Orca's worktree-management flow to create a new Orca-managed worktree for `main`.\\n* Do not use an ad hoc `git worktree add` as a substitute for creating the worktree in Orca.\\n* Perform the post-cleanup work from the new Orca `main` worktree.\\n* Preserve the current worktree until all work has been committed, pushed, integrated, and verified safe to remove.\\n* Use the repository's existing Orca conventions and the `orca-cli` workflow for naming, locating, and selecting the new worktree.\\nIf this is not being performed through the Orca CLI, use the repository's normal worktree workflow.\\n## 1. Land the plane first\\n* Uncommitted changes: Commit them, and push if that is the repository norm. Skip this step if the worktree is clean.\\n* Integration: Push the branch. Open a PR if none exists. Merge the PR when ready and appropriate, using `gh` and project conventions.\\n* Prefer merging over leaving an open branch when it is safe to do so.\\n* Worktree safety: Treat the worktree as part of the branch's active state. Only remove it after the branch is merged or safely discarded.\\n* Never remove a worktree that contains the only copy of unpushed or unmerged work without my explicit confirmation.\\n## 2. Record the current state\\nBefore changing anything, note:\\n* The current branch name.\\n* The current worktree path.\\n* Whether this is a linked worktree.\\n* The current commit and whether the worktree is clean.\\n## 3. If already on `main`\\nIf the current worktree is already on `main`:\\n* Do not create unnecessary cleanup work.\\n* Stop and report that cleanup is complete or not applicable.\\n* Only pull or perform additional cleanup if required by the repository state.\\n## 4. Move to `main`\\n* If using the Orca CLI, create and select the new Orca-managed `main` worktree as described above.\\n* Otherwise, switch to `main` from the primary worktree if the feature worktree was removed.\\n* Do not force a checkout that would overwrite local changes.\\n## 5. Pull the latest `main`\\nFrom the `main` worktree:\\n* Pull the latest changes so local `main` matches the remote.\\n* Confirm the pull completed successfully.\\n* Report any divergence or conflicts instead of silently proceeding.\\n## 6. Clean up the feature branch\\nBefore deleting the local branch:\\n* Run `git worktree list`.\\n* Check whether any sibling worktrees still have the feature branch checked out.\\n* Confirm the branch has been merged upstream or was explicitly discarded.\\nIf no sibling worktree has the branch checked out:\\n* Delete the local branch once it is merged upstream.\\n* Prune stale remote-tracking references if useful and safe.\\nIf a sibling worktree still has the branch checked out:\\n* Report these as two separate outcomes:\\n    1. Merge result: Whether PR or branch integration succeeded.\\n    2. Local cleanup debt: The blocking worktree path, branch, current commit, and exact cleanup command.\\nIf the sibling worktree is safe to remove—meaning it is merged or explicitly discardable, has no unique unpushed work, and has no uncommitted changes—prefer the repository's normal cleanup command, such as `wt remove <branch>` or `git worktree remove <path>`, then delete the branch.\\n\\nIf it is not safe to remove:\\n* Stop after reporting the exact path and cleanup command.\\n* Do not treat blocked local deletion as a merge failure.\\n* Do not delete the branch or worktree.\\n\\nOnce you create the new worktree, pin it in Orca\");"
  },
  "targets": [
    "raycast-snippet"
  ],
  "x": {
    "raycast": {
      "snippet": {
        "text": "I'm done with this branch. Close it out safely, integrate it, and move me onto `main`.\nDefault: land the plane first. Always finish integration before local cleanup. Do not delete branches or remove worktrees while work is still uncommitted, unmerged, or unpushed unless I explicitly say otherwise.\n## Orca CLI requirement\nIf this task is being performed through the Orca CLI:\n* Use Orca's worktree-management flow to create a new Orca-managed worktree for `main`.\n* Do not use an ad hoc `git worktree add` as a substitute for creating the worktree in Orca.\n* Perform the post-cleanup work from the new Orca `main` worktree.\n* Preserve the current worktree until all work has been committed, pushed, integrated, and verified safe to remove.\n* Use the repository's existing Orca conventions and the `orca-cli` workflow for naming, locating, and selecting the new worktree.\nIf this is not being performed through the Orca CLI, use the repository's normal worktree workflow.\n## 1. Land the plane first\n* Uncommitted changes: Commit them, and push if that is the repository norm. Skip this step if the worktree is clean.\n* Integration: Push the branch. Open a PR if none exists. Merge the PR when ready and appropriate, using `gh` and project conventions.\n* Prefer merging over leaving an open branch when it is safe to do so.\n* Worktree safety: Treat the worktree as part of the branch's active state. Only remove it after the branch is merged or safely discarded.\n* Never remove a worktree that contains the only copy of unpushed or unmerged work without my explicit confirmation.\n## 2. Record the current state\nBefore changing anything, note:\n* The current branch name.\n* The current worktree path.\n* Whether this is a linked worktree.\n* The current commit and whether the worktree is clean.\n## 3. If already on `main`\nIf the current worktree is already on `main`:\n* Do not create unnecessary cleanup work.\n* Stop and report that cleanup is complete or not applicable.\n* Only pull or perform additional cleanup if required by the repository state.\n## 4. Move to `main`\n* If using the Orca CLI, create and select the new Orca-managed `main` worktree as described above.\n* Otherwise, switch to `main` from the primary worktree if the feature worktree was removed.\n* Do not force a checkout that would overwrite local changes.\n## 5. Pull the latest `main`\nFrom the `main` worktree:\n* Pull the latest changes so local `main` matches the remote.\n* Confirm the pull completed successfully.\n* Report any divergence or conflicts instead of silently proceeding.\n## 6. Clean up the feature branch\nBefore deleting the local branch:\n* Run `git worktree list`.\n* Check whether any sibling worktrees still have the feature branch checked out.\n* Confirm the branch has been merged upstream or was explicitly discarded.\nIf no sibling worktree has the branch checked out:\n* Delete the local branch once it is merged upstream.\n* Prune stale remote-tracking references if useful and safe.\nIf a sibling worktree still has the branch checked out:\n* Report these as two separate outcomes:\n    1. Merge result: Whether PR or branch integration succeeded.\n    2. Local cleanup debt: The blocking worktree path, branch, current commit, and exact cleanup command.\nIf the sibling worktree is safe to remove—meaning it is merged or explicitly discardable, has no unique unpushed work, and has no uncommitted changes—prefer the repository's normal cleanup command, such as `wt remove <branch>` or `git worktree remove <path>`, then delete the branch.\n\nIf it is not safe to remove:\n* Stop after reporting the exact path and cleanup command.\n* Do not treat blocked local deletion as a merge failure.\n* Do not delete the branch or worktree.\n\nOnce you create the new worktree, pin it in Orca",
        "keyword": ";co"
      }
    }
  },
  "author": "polycast capture"
});
