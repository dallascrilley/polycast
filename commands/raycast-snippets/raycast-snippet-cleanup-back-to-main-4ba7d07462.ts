// Generated by `polycast capture --from raycast-snippets`.
// The source export stays outside Git. Regenerate this file instead of editing it.

import { defineCommand } from "../../src/define.ts";

export default defineCommand({
  "id": "raycast-snippet-cleanup-back-to-main-4ba7d07462",
  "title": "Cleanup back to main",
  "description": "Static text captured from a Raycast snippet export.",
  "modality": "none",
  "body": {
    "lang": "node",
    "source": "process.stdout.write(\"I'm done with this branch. Close it out safely, switch to main, and clean up.\\nDefault: land the plane first. Always finish integration before local cleanup. Do not delete branches or remove worktrees while work is still uncommitted/unmerged/unpushed (unless I explicitly say otherwise).\\n### 1. Land the plane (do this first)\\n- Uncommitted changes: Commit (and push if that’s the repo norm). Skip if clean.\\n- Integration: Push the branch. Open a PR if none exists. Merge the PR when ready and appropriate (use `gh` + project conventions). Prefer merging over leaving an open branch when safe.\\n- Worktrees: Treat the worktree like the branch. Only remove it (`git worktree remove` or `wt remove`) after the branch is merged (or safely discarded). Never remove a worktree that holds the only copy of unpushed/unmerged work without explicit confirmation.\\n### 2. Record current state\\n- Note the current branch name.\\n- Note the worktree path if this is a linked worktree.\\n### 3. Already on main?\\nIf already on `main`, stop and report. Cleanup may already be done or N/A.\\n### 4. Switch to main\\n- Check out `main` (from the primary worktree if you just removed a linked one).\\n### 5. Pull latest\\n- On `main`, pull so local matches remote.\\n### 6. Clean up the feature branch\\nBefore deleting the local branch:\\n- Run `git worktree list` and check for any sibling worktrees still on this branch.\\n- If no siblings have it checked out: Delete the local branch once it is merged upstream. Prune stale remote-tracking refs if useful.\\n- If a sibling worktree still has the branch:\\n  - Report two separate things:\\n    1. Merge result — whether the PR/branch integration succeeded.\\n    2. Local cleanup debt — the blocking worktree path, branch, current commit, and the exact cleanup command.\\n  - If the sibling is safe to remove (merged or explicitly discardable, no unique unpushed work, no uncommitted changes): prefer `wt remove <branch>` or `git worktree remove <path>`, then delete the branch.\\n  - If not safe: stop after reporting the exact path + cleanup command. Do not treat a blocked local delete as a merge failure.\");"
  },
  "targets": [
    "raycast-snippet"
  ],
  "x": {
    "raycast": {
      "snippet": {
        "text": "I'm done with this branch. Close it out safely, switch to main, and clean up.\nDefault: land the plane first. Always finish integration before local cleanup. Do not delete branches or remove worktrees while work is still uncommitted/unmerged/unpushed (unless I explicitly say otherwise).\n### 1. Land the plane (do this first)\n- Uncommitted changes: Commit (and push if that’s the repo norm). Skip if clean.\n- Integration: Push the branch. Open a PR if none exists. Merge the PR when ready and appropriate (use `gh` + project conventions). Prefer merging over leaving an open branch when safe.\n- Worktrees: Treat the worktree like the branch. Only remove it (`git worktree remove` or `wt remove`) after the branch is merged (or safely discarded). Never remove a worktree that holds the only copy of unpushed/unmerged work without explicit confirmation.\n### 2. Record current state\n- Note the current branch name.\n- Note the worktree path if this is a linked worktree.\n### 3. Already on main?\nIf already on `main`, stop and report. Cleanup may already be done or N/A.\n### 4. Switch to main\n- Check out `main` (from the primary worktree if you just removed a linked one).\n### 5. Pull latest\n- On `main`, pull so local matches remote.\n### 6. Clean up the feature branch\nBefore deleting the local branch:\n- Run `git worktree list` and check for any sibling worktrees still on this branch.\n- If no siblings have it checked out: Delete the local branch once it is merged upstream. Prune stale remote-tracking refs if useful.\n- If a sibling worktree still has the branch:\n  - Report two separate things:\n    1. Merge result — whether the PR/branch integration succeeded.\n    2. Local cleanup debt — the blocking worktree path, branch, current commit, and the exact cleanup command.\n  - If the sibling is safe to remove (merged or explicitly discardable, no unique unpushed work, no uncommitted changes): prefer `wt remove <branch>` or `git worktree remove <path>`, then delete the branch.\n  - If not safe: stop after reporting the exact path + cleanup command. Do not treat a blocked local delete as a merge failure.",
        "keyword": ";cl"
      }
    }
  },
  "author": "polycast capture"
});
