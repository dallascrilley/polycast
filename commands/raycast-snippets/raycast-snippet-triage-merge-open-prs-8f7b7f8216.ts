// Generated by `polycast capture --from raycast-snippets`.
// The source export stays outside Git. Regenerate this file instead of editing it.

import { defineCommand } from "../../src/define.ts";

export default defineCommand({
  "id": "raycast-snippet-triage-merge-open-prs-8f7b7f8216",
  "title": "Triage & Merge Open PRs",
  "description": "Static text captured from a Raycast snippet export.",
  "modality": "none",
  "body": {
    "lang": "node",
    "source": "process.stdout.write(\"# Role & Objective\\nYou are an advanced, repo-aware AI coding agent. Your task is to orchestrate a safe, automated workflow to review, resolve, and merge all open Pull Requests (PRs) in this repository. \\nAdapt any command syntax to your specific runtime environment as needed. Use relevant local `~/.hub` skills if available, such as `plan`, `code-reviewer`, `verification-loop`, and `receiving-code-review`.\\n---\\n## Step 1: Start-of-Turn Repo Hygiene & Discovery\\nBefore modifying any files or executing the workflow, inspect the local environment:\\n1. Run the following command to assess the current state:\\n   ```bash\\n   git status --short && git branch --show-current && git rev-parse --show-toplevel\\n   ```\\n2. If the repository is dirty, classify existing changes before proceeding. Do not mix unrelated existing work with this task. Isolate your work using branches or worktrees.\\n3. Discover the environment context:\\n   - Identify the tech stack, package manager, and test runner.\\n   - Look for instruction files (e.g., `CLAUDE.md`, `.cursorrules`, `DEVELOPER.md`).\\n   - Check if a platform CLI is installed and authenticated (e.g., GitHub CLI `gh` or GitLab CLI `glab`). If no CLI is available, check if you can access PR data via Git metadata, APIs, or local configuration.\\n---\\n## Step 2: Planning & Dependency Analysis\\nCreate a execution plan before acting:\\n1. List Open PRs: Retrieve a list of all active, open PRs for this repository.\\n2. Determine Optimal Merge Order: Analyze the changes, modified files, and dependency graph across these PRs. Identify potential conflicts, shared modules, or logical dependencies to establish a safe, serial merge order (e.g., independent utility PRs first, complex feature PRs last).\\n3. Draft the Plan: Present the proposed merge sequence, along with your reasoning, before executing.\\n---\\n## Step 3: Execution Loop (Iterate per PR)\\nFor each open PR, in the optimal order determined in Step 2, execute the following sub-workflow:\\n### A. Worktree Setup\\n1. Create a clean, isolated git worktree for the PR's source branch. Do not perform edits directly on your main working directory to avoid polluting the state.\\n2. Shallow clone or fetch the branch as necessary to minimize disk and network overhead.\\n### B. Feedback Retrieval & Local Review\\n1. Retrieve PR Feedback: Fetch existing comments, review threads, and CI failure logs from the hosting platform (e.g., using `gh pr view`).\\n2. Run Code-Reviewer Subagent: Run an internal review or invoke your `code-reviewer` skill on the diff to identify syntax errors, anti-patterns, security issues, or logical flaws.\\n### C. Resolution & Verification Loop\\n1. Fix Findings: Resolve all findings from both the retrieved PR comments and your code-reviewer analysis.\\n2. Local Verification: Discover and run the project's verification commands (e.g., unit tests, linter, typecheck, build step) inside the worktree to ensure your fixes do not break existing behavior.\\n3. Commit & Push: Commit your fixes with clear, descriptive messages and push them to the remote PR branch.\\n4. Resolve Comments: Mark the addressed review comments as resolved on the remote platform if CLI capabilities permit.\\n---\\n## Step 4: Merging\\nOnce a PR is fully resolved, verified, and clean:\\n1. Merge the PR into the target branch (e.g., `main` or `master`) using the determined merge strategy (squash, rebase, or merge commit, matching the repository's convention).\\n2. Delete the remote and local PR branches, and prune the associated git worktree.\\n3. Pull the updated target branch before starting the next PR in the sequence to ensure subsequent PRs are evaluated against the latest codebase.\\n---\\n## Step 5: Verification & Handoff\\nAfter processing all PRs, provide a comprehensive handoff summary:\\n1. Summary of Changes: Detail which PRs were processed, what fixes were applied, and their final status.\\n2. Verification Results: List the test and lint commands run during the process and confirm they passed.\\n3. Unresolved Risks: Note any remaining merge conflicts, failing CI jobs, or manual review items that require human intervention.\\n4. Final Git Status: Run a final git status check to confirm the repository is clean and in the correct state:\\n   ```bash\\n   git status --short && git branch --show-current\\n   ```\\n```\");"
  },
  "targets": [
    "raycast-snippet"
  ],
  "x": {
    "raycast": {
      "snippet": {
        "text": "# Role & Objective\nYou are an advanced, repo-aware AI coding agent. Your task is to orchestrate a safe, automated workflow to review, resolve, and merge all open Pull Requests (PRs) in this repository. \nAdapt any command syntax to your specific runtime environment as needed. Use relevant local `~/.hub` skills if available, such as `plan`, `code-reviewer`, `verification-loop`, and `receiving-code-review`.\n---\n## Step 1: Start-of-Turn Repo Hygiene & Discovery\nBefore modifying any files or executing the workflow, inspect the local environment:\n1. Run the following command to assess the current state:\n   ```bash\n   git status --short && git branch --show-current && git rev-parse --show-toplevel\n   ```\n2. If the repository is dirty, classify existing changes before proceeding. Do not mix unrelated existing work with this task. Isolate your work using branches or worktrees.\n3. Discover the environment context:\n   - Identify the tech stack, package manager, and test runner.\n   - Look for instruction files (e.g., `CLAUDE.md`, `.cursorrules`, `DEVELOPER.md`).\n   - Check if a platform CLI is installed and authenticated (e.g., GitHub CLI `gh` or GitLab CLI `glab`). If no CLI is available, check if you can access PR data via Git metadata, APIs, or local configuration.\n---\n## Step 2: Planning & Dependency Analysis\nCreate a execution plan before acting:\n1. List Open PRs: Retrieve a list of all active, open PRs for this repository.\n2. Determine Optimal Merge Order: Analyze the changes, modified files, and dependency graph across these PRs. Identify potential conflicts, shared modules, or logical dependencies to establish a safe, serial merge order (e.g., independent utility PRs first, complex feature PRs last).\n3. Draft the Plan: Present the proposed merge sequence, along with your reasoning, before executing.\n---\n## Step 3: Execution Loop (Iterate per PR)\nFor each open PR, in the optimal order determined in Step 2, execute the following sub-workflow:\n### A. Worktree Setup\n1. Create a clean, isolated git worktree for the PR's source branch. Do not perform edits directly on your main working directory to avoid polluting the state.\n2. Shallow clone or fetch the branch as necessary to minimize disk and network overhead.\n### B. Feedback Retrieval & Local Review\n1. Retrieve PR Feedback: Fetch existing comments, review threads, and CI failure logs from the hosting platform (e.g., using `gh pr view`).\n2. Run Code-Reviewer Subagent: Run an internal review or invoke your `code-reviewer` skill on the diff to identify syntax errors, anti-patterns, security issues, or logical flaws.\n### C. Resolution & Verification Loop\n1. Fix Findings: Resolve all findings from both the retrieved PR comments and your code-reviewer analysis.\n2. Local Verification: Discover and run the project's verification commands (e.g., unit tests, linter, typecheck, build step) inside the worktree to ensure your fixes do not break existing behavior.\n3. Commit & Push: Commit your fixes with clear, descriptive messages and push them to the remote PR branch.\n4. Resolve Comments: Mark the addressed review comments as resolved on the remote platform if CLI capabilities permit.\n---\n## Step 4: Merging\nOnce a PR is fully resolved, verified, and clean:\n1. Merge the PR into the target branch (e.g., `main` or `master`) using the determined merge strategy (squash, rebase, or merge commit, matching the repository's convention).\n2. Delete the remote and local PR branches, and prune the associated git worktree.\n3. Pull the updated target branch before starting the next PR in the sequence to ensure subsequent PRs are evaluated against the latest codebase.\n---\n## Step 5: Verification & Handoff\nAfter processing all PRs, provide a comprehensive handoff summary:\n1. Summary of Changes: Detail which PRs were processed, what fixes were applied, and their final status.\n2. Verification Results: List the test and lint commands run during the process and confirm they passed.\n3. Unresolved Risks: Note any remaining merge conflicts, failing CI jobs, or manual review items that require human intervention.\n4. Final Git Status: Run a final git status check to confirm the repository is clean and in the correct state:\n   ```bash\n   git status --short && git branch --show-current\n   ```\n```"
      }
    }
  },
  "author": "polycast capture"
});
