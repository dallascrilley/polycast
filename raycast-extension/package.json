{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "polycast",
  "title": "Polycast",
  "description": "Browse and run polycast commands with live argument pickers.",
  "icon": "icon.png",
  "author": "dallascrilley",
  "license": "MIT",
  "commands": [
    {
      "name": "run-command",
      "title": "Run Polycast Command",
      "subtitle": "Polycast",
      "description": "Pick a command from the polycast store, fill its arguments, and run it.",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "polycastBin",
      "type": "textfield",
      "required": false,
      "title": "polycast binary",
      "description": "Path to the polycast CLI.",
      "default": "~/.local/bin/polycast"
    },
    {
      "name": "commandsDir",
      "type": "textfield",
      "required": false,
      "title": "Commands store",
      "description": "Directory of built polycast command JSON documents.",
      "default": "~/.polycast/commands"
    },
    {
      "name": "orcaBin",
      "type": "textfield",
      "required": false,
      "title": "orca binary",
      "description": "Path to the orca CLI, used by the worktree picker.",
      "default": "/usr/local/bin/orca"
    },
    {
      "name": "extraPath",
      "type": "textfield",
      "required": false,
      "title": "Extra PATH",
      "description": "Colon-separated directories appended to PATH when running commands, since Raycast's own PATH is minimal.",
      "default": "/opt/homebrew/bin:/usr/local/bin:~/.local/bin:~/.bun/bin"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.104.25"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "typescript": "^5.8.2"
  },
  "scripts": {
    "dev": "ray develop",
    "build": "ray build -e dist",
    "typecheck": "tsc --noEmit"
  }
}
