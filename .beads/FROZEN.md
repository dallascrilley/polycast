# Frozen Beads ledger

This `.beads/` tree is recovery evidence. It is not the execution board.

Selector: `project.yaml` `tracking.system: linear`.
State: `legacy.beads.state: frozen`.
Receipt: `legacy.beads.snapshot_receipt` (absolute path and SHA-256).

Inspect the snapshot named there. Do not create, update, claim, or close work
in this tree. Linear owns live work state.
